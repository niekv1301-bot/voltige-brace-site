/components/ui/
├─ Button.js
├─ Card.js
└─ CardContent.js

import React from "react";
import clsx from "clsx";

export function Button({ children, className, variant, ...props }) {
  const base = "inline-flex items-center justify-center font-medium transition-colors focus:outline-none focus:ring-2 focus:ring-offset-2";
  
  const variants = {
    default: "bg-indigo-600 text-white hover:bg-indigo-700",
    outline: "border border-gray-300 text-gray-700 hover:bg-gray-100",
  };

  return (
    <button
      className={clsx(base, variant ? variants[variant] : variants.default, className)}
      {...props}
    >
      {children}
    </button>
  );
}

import React from "react";
import clsx from "clsx";

export function Card({ children, className, ...props }) {
  return (
    <div className={clsx("bg-white rounded-2xl shadow-md p-0 transition", className)} {...props}>
      {children}
    </div>
  );
}

import React from "react";
import clsx from "clsx";

export function CardContent({ children, className, ...props }) {
  return (
    <div className={clsx("p-4", className)} {...props}>
      {children}
    </div>
  );
}

npm install clsx

/voltige-braces/
├─ package.json
├─ next.config.js
├─ tailwind.config.js
├─ postcss.config.js
├─ styles/globals.css
├─ pages/index.js
├─ components/ui/Button.js
├─ components/ui/Card.js
├─ components/ui/CardContent.js
└─ public/images/ (hero.png, kneebrace.png, anklebrace.png, wristguard.png)

